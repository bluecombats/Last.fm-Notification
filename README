Sending notifications from Last.fm website. To install make sure you have a the greasemonkey or Tampermonkey plugin installed, then click on the .user.js file and then click on the raw file, it should install from there.